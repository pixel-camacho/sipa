import { OnFulfilled, OnRejected, Promise } from 'rsvp';
import { Dict, Maybe } from './core';
import HandlerInfo, { IHandler } from './handler-info';
import Router from './router';
import { TransitionIntent } from './transition-intent';
import TransitionState from './transition-state';
export { default as TransitionAborted } from './transition-aborted-error';
/**
  A Transition is a thennable (a promise-like object) that represents
  an attempt to transition to another route. It can be aborted, either
  explicitly via `abort` or by attempting another transition while a
  previous one is still underway. An aborted transition can also
  be `retry()`d later.

  @class Transition
  @constructor
  @param {Object} router
  @param {Object} intent
  @param {Object} state
  @param {Object} error
  @private
 */
export declare class Transition {
    state?: TransitionState;
    router: Router;
    data: Dict<unknown>;
    intent?: TransitionIntent;
    resolvedModels: Dict<Dict<unknown> | undefined>;
    queryParams: Dict<unknown>;
    promise?: Promise<any>;
    error: Maybe<Error>;
    params: Dict<unknown>;
    handlerInfos: HandlerInfo[];
    targetName: Maybe<string>;
    pivotHandler: Maybe<IHandler>;
    sequence: number;
    isAborted: boolean;
    isActive: boolean;
    urlMethod: string;
    resolveIndex: number;
    queryParamsOnly: boolean;
    isTransition: boolean;
    isCausedByAbortingTransition: boolean;
    isCausedByInitialTransition: boolean;
    isCausedByAbortingReplaceTransition: boolean;
    _visibleQueryParams: Dict<unknown>;
    constructor(router: Router, intent: TransitionIntent | undefined, state: TransitionState | undefined, error?: Maybe<Error>, previousTransition?: Maybe<Transition>);
    isExiting(handler: IHandler | string): boolean;
    /**
      The Transition's internal promise. Calling `.then` on this property
      is that same as calling `.then` on the Transition object itself, but
      this property is exposed for when you want to pass around a
      Transition's promise, but not the Transition object itself, since
      Transition object can be externally `abort`ed, while the promise
      cannot.
  
      @property promise
      @type {Object}
      @public
     */
    /**
      Custom state can be stored on a Transition's `data` object.
      This can be useful for decorating a Transition within an earlier
      hook and shared with a later hook. Properties set on `data` will
      be copied to new transitions generated by calling `retry` on this
      transition.
  
      @property data
      @type {Object}
      @public
     */
    /**
      A standard promise hook that resolves if the transition
      succeeds and rejects if it fails/redirects/aborts.
  
      Forwards to the internal `promise` property which you can
      use in situations where you want to pass around a thennable,
      but not the Transition itself.
  
      @method then
      @param {Function} onFulfilled
      @param {Function} onRejected
      @param {String} label optional string for labeling the promise.
      Useful for tooling.
      @return {Promise}
      @public
     */
    then<T>(onFulfilled: OnFulfilled<TransitionState | undefined | Error, T>, onRejected: OnRejected<TransitionState, T>, label: string): Promise<T>;
    /**
  
      Forwards to the internal `promise` property which you can
      use in situations where you want to pass around a thennable,
      but not the Transition itself.
  
      @method catch
      @param {Function} onRejection
      @param {String} label optional string for labeling the promise.
      Useful for tooling.
      @return {Promise}
      @public
     */
    catch<T>(onRejection: OnRejected<TransitionState, T>, label: string): Promise<any>;
    /**
  
      Forwards to the internal `promise` property which you can
      use in situations where you want to pass around a thennable,
      but not the Transition itself.
  
      @method finally
      @param {Function} callback
      @param {String} label optional string for labeling the promise.
      Useful for tooling.
      @return {Promise}
      @public
     */
    finally<T>(callback: T | undefined, label?: string): Promise<any>;
    /**
      Aborts the Transition. Note you can also implicitly abort a transition
      by initiating another transition while a previous one is underway.
  
      @method abort
      @return {Transition} this transition
      @public
     */
    abort(): this;
    /**
  
      Retries a previously-aborted transition (making sure to abort the
      transition if it's still active). Returns a new transition that
      represents the new attempt to transition.
  
      @method retry
      @return {Transition} new transition
      @public
     */
    retry(): any;
    /**
  
      Sets the URL-changing method to be employed at the end of a
      successful transition. By default, a new Transition will just
      use `updateURL`, but passing 'replace' to this method will
      cause the URL to update using 'replaceWith' instead. Omitting
      a parameter will disable the URL change, allowing for transitions
      that don't update the URL at completion (this is also used for
      handleURL, since the URL has already changed before the
      transition took place).
  
      @method method
      @param {String} method the type of URL-changing method to use
        at the end of a transition. Accepted values are 'replace',
        falsy values, or any other non-falsy value (which is
        interpreted as an updateURL transition).
  
      @return {Transition} this transition
      @public
     */
    method(method: string): this;
    send(ignoreFailure: boolean, _name: string, err?: Error, transition?: Transition, handler?: IHandler): void;
    /**
  
      Fires an event on the current list of resolved/resolving
      handlers within this transition. Useful for firing events
      on route hierarchies that haven't fully been entered yet.
  
      Note: This method is also aliased as `send`
  
      @method trigger
      @param {Boolean} [ignoreFailure=false] a boolean specifying whether unhandled events throw an error
      @param {String} name the name of the event to fire
      @public
     */
    trigger(ignoreFailure: boolean, _name: string, ...args: any[]): void;
    /**
      Transitions are aborted and their promises rejected
      when redirects occur; this method returns a promise
      that will follow any redirects that occur and fulfill
      with the value fulfilled by any redirecting transitions
      that occur.
  
      @method followRedirects
      @return {Promise} a promise that fulfills with the same
        value that the final redirecting transition fulfills with
      @public
     */
    followRedirects(): Promise<unknown>;
    toString(): string;
    /**
      @private
     */
    log(message: string): void;
}
/**
  @private

  Logs and returns an instance of TransitionAborted.
 */
export declare function logAbort(transition: Transition): import("router/transition-aborted-error").TransitionAbortedError;
export declare function isTransition(obj: Dict<unknown> | undefined): obj is Transition;
export declare function prepareResult(obj: Dict<unknown> | undefined): Dict<unknown> | null | undefined;
